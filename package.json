{
  "name": "bb-check-workspace",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "packageManager": "npm@11.19.0",
  "engines": {
    "node": ">=22.12"
  },
  "scripts": {
    "build": "turbo run build",
    "build:release": "turbo run build --force",
    "test": "vitest run --exclude \"scripts/**\"",
    "test:scripts": "node --test scripts/*.test.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit && turbo run typecheck",
    "typecheck:release": "tsc -p tsconfig.json --noEmit && turbo run typecheck --force",
    "lint": "eslint tests scripts --no-error-on-unmatched-pattern && turbo run lint",
    "lint:release": "eslint tests scripts --no-error-on-unmatched-pattern && turbo run lint --force",
    "format:check": "prettier --check .",
    "check-package-files": "node scripts/check-package-files.mjs",
    "test-packed-package": "node scripts/test-packed-package.mjs",
    "check-public-words": "node scripts/check-public-words.mjs",
    "check": "npm run format:check && npm run lint && npm run typecheck && npm run build && npm test && npm run check-package-files && npm run test-packed-package",
    "verify:package-release": "npm run format:check && npm run lint:release && npm run typecheck:release && npm run build:release && npm test && npm run check-package-files && npm run test-packed-package",
    "verify:release": "npm run verify:package-release && npm run test:scripts",
    "publish:npm": "node scripts/publish-package.mjs"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@types/node": "^22.20.1",
    "@typescript/native": "npm:typescript@7.0.2",
    "@typescript/typescript6": "6.0.2",
    "eslint": "10.9.0",
    "prettier": "3.9.6",
    "rollup": "4.62.5",
    "rollup-plugin-dts": "6.5.1",
    "turbo": "2.10.11",
    "typescript": "npm:@typescript/typescript6@6.0.2",
    "typescript-eslint": "8.67.0",
    "vite": "8.2.2",
    "vitest": "4.1.11"
  }
}

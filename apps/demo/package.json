{
  "name": "bb-check-demo",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build && node --experimental-strip-types scripts/build-fixtures.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint . --no-error-on-unmatched-pattern"
  },
  "devDependencies": {
    "@cp949/bb-check": "0.1.0"
  }
}

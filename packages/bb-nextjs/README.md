# @cp949/bb-nextjs

`0.1.0` 범위: private manifest와 TypeScript 설정만 존재한다. 공개 export, stub 구현은 두지 않는다.

Next.js 통합(webpack adapter, runtime adapter)은 실제 두 adapter 요구사항이 확정된 후 `0.2.0`부터 구현한다. 이 패키지는 `@cp949/bb-check`, `@cp949/bb-library`와 별도로 배포되지 않으며, 현재는 workspace 내부에만 존재한다.
